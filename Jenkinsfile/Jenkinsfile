pipeline {
    agent any 

    environment {
        RELEASE = "2.0"
    }

    stages {
        agent any 
        environment{
            LOG_LEVEL = "INFO"
        }
        stage('clone') {
            steps {
                sh '''
                    echo "clone release version ${RELEASE}  and log level ${LOG_LEVEL}"
                '''
            }
        }

        stage('Build') {
            steps {
                sh '''
                    echo "building"
                '''
            }
        }
    }
}

